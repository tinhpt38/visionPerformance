<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tinhphan.visionperformance">

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
            android:name=".AppInstance"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".Landing.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".AnalyzerResult.AnalyzerResultActivity">
        </activity>
        <activity android:name=".Cameraman.CameramanActivity">
        </activity>
        <activity android:name=".Analyzing.AnalyzingActivity"
            android:label=""
            android:theme="@style/ActivityDialog">
        </activity>
    </application>

</manifest>